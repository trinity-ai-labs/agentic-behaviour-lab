{
  "name": "agentic-behaviour-lab",
  "private": true,
  "version": "0.1.0",
  "description": "Open lab for measuring AI coding-agent behaviour: scenarios, trials, benchmarks, dashboard, MCP.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "pre-commit": "pnpm check",
    "gate": "pnpm build && pnpm check && pnpm test",
    "gate:enqueue": "node scripts/enqueue-gate.mjs",
    "gate:drain": "node scripts/gate-runner.mjs"
  }
}
